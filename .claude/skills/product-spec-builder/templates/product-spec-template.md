---
name: product-spec-template
description: Product Spec 输出模板。当需要生成产品需求文档时，按照此模板的结构和格式填充内容，输出为 Product-Spec.md 文件。
---

# Product Spec 输出模板

本模板用于生成结构完整的 Product Spec 文档。生成时按照此结构填充内容。

---

## 模板结构

**文件命名**：Product-Spec.md

---

## 产品概述
<一段话说清楚：>
- 这是什么产品
- 解决什么问题
- **目标用户是谁**（具体描述，不要只说「用户」）
- 核心价值是什么

## 术语表
<定义本文档中所有专有术语的精确含义，确保跨 Phase 沟通无歧义。格式：>

| 术语 | 英文 | 定义 | 禁止混用 |
|------|------|------|---------|
| <中文术语> | <English> | <在本产品语境下的精确定义> | <不应使用的别名/旧称> |

<要求：>
- 每个核心概念必须有唯一定义
- 全文统一使用"术语"列的名称，"禁止混用"列的词汇不得出现
- 涉及 AI/技术/产品特有概念时必须定义
- 示例：
  | 阶段卡片 | Stage Card | 左侧导航区 4 个固定创作阶段卡片（故事大纲/人物小传/章节目录/章节写作）| 阶段流程卡片、流程卡片 |
  | Skill | Skill | 用户导入或新建的题材技能包，含 CLAUDE.md + skills/ + 模板 + 示例 | 技能包、题材包 |

## 应用场景
<列举 3-5 个具体场景：谁、在什么情况下、怎么用、解决什么问题>

## 功能需求
<按「核心功能」和「辅助功能」分类。每条功能包含三要素：>

**功能描述**: 用户做什么 → 系统做什么 → 得到什么
**验收标准**: 
  - [ ] <可度量的条件，如"标签页切换响应时间 < 20ms">
  - [ ] <可度量的条件，如"支持同时打开 ≥ 20 个标签页不崩溃">
  - [ ] <可度量的条件，如"空状态显示引导文案+行动按钮">
<验收标准 = 客观、可测量、无歧义。不用"体验好""反应快"等主观描述>

## UI 布局
<描述整体布局结构和各区域的详细设计，需要包含：>
- 整体是什么布局（几栏、比例、固定元素等）
- 每个区域放什么内容
- 控件的具体规范（位置、尺寸、样式等）

## 用户使用流程
<分步骤描述用户如何使用产品，可以有多条路径（如快速上手、进阶使用）>

## AI 能力需求

| 能力类型 | 用途说明 | 应用位置 |
|---------|---------|---------|
| <能力类型> | <做什么> | <在哪个环节触发> |

## 技术方向

| 维度 | 选择 | 理由 |
|------|------|------|
| 产品类型 | <Web / Desktop / CLI / Mobile> | <为什么选这个平台> |
| 推荐技术栈 | <如 Next.js + TypeScript + Tailwind> | <为什么选这套技术> |
| 数据存储 | <本地 / 云端 / 混合> | <基于产品需求的判断> |
| 部署方式 | <静态托管 / 服务器 / 桌面安装包 / App Store> | <根据产品类型> |

## 绑定文件结构清单

<若产品有"文件→功能"的绑定关系（如阶段卡片绑定 .md 文件），填写此表。审计管线自动检查文件结构完整性。无绑定文件的产品填"不适用"——管线自动跳过。>

| 绑定文件 | 用途 | 必须包含的结构关键词 | 首行应匹配 |
|---------|------|-------------------|-----------|
| <文件名> | <对应功能> | <关键词1, 关键词2, 关键词3> | <首行正则> |

<示例——写作工具:>
| outline.md | 故事大纲 | 故事大纲, 故事主线, 核心冲突, 故事调性 | 故事大纲 |
| character.md | 人物小传 | 人物小传, 男主角, 女主角 | 人物小传 |
| chapter-index.md | 章节目录 | 章节目录 | 章节目录 |

## 风险与应对策略

<识别产品中的关键风险，每项定义触发条件和应对策略。D5 审计仅检查此表。>

| 风险 | 触发条件 | 应对策略 | 状态 |
|------|---------|---------|:--:|
| <风险名称> | <何时触发> | <系统如何应对> | ⏳ |


## 非功能需求

<对照 ISO 25010 标准，逐类覆盖。不涉及的类型标注"不适用"及理由。>

### 性能
| 操作 | P50 | P90 | P99 | CI红线 | 测量方式 |
|------|-----|-----|-----|--------|---------|
| <核心操作> | <ms> | <ms> | <ms> | <ms> | <工具> |

### 安全性
- 数据传输：<加密方式 / 不适用理由>
- 数据存储：<加密方式 / 不适用理由>
- 认证方式：<方式 / 不适用理由>
- API Key 管理：<存储方案 / 不适用理由>

### 可访问性 (a11y)
- 色彩对比度：<WCAG AA / AAA / 不适用理由>
- 键盘导航：<全覆盖 / 部分覆盖 / 不适用理由>
- 屏幕阅读器：<支持 / 不适用理由>
- reduced-motion：<支持 / 不适用理由>

### 国际化 (i18n)
- v1 目标语言：<中文 / 英文 / 多语言>
- UI 字符串管理：<集中管理 / 硬编码 / 不适用>
- 扩展计划：<v2 多语言 / 无计划>

### 合规
- 隐私政策：<有 / 模板 / 不需要理由>
- 数据导出/删除：<支持 / 不适用理由>
- 用户数据收集范围：<明确列出 / 不收集>

## 导航状态矩阵

<有页面路由/视图切换的产品必须填写。定义每次页面跳转时的状态保留策略。>

| 从 → 到 | 滚动位置 | 展开/折叠 | 输入内容 | 选中项 | 异步操作 |
|---------|:--:|:--:|:--:|:--:|:--:|
| <页面A> → <页面B> → 返回A | 保留/重置 | 保留/重置 | 保留/重置 | 保留/重置 | 等待完成/中断提示/后台继续 |

<示例——写作工具:>
| 从 → 到 | 滚动位置 | 章节目录展开 | 搜索词 | 选中章节 |
|---------|:--:|:--:|:--:|:--:|
| 章节目录→打开章节→[←目录]返回 | 保留 | 保留 | 保留 | 保留 |

## 多入口一致性

<同一页面有多个到达入口的产品必须填写。验证不同入口的行为一致。>

| 目标页面 | 入口1 | 入口2 | 行为差异处理 |
|---------|------|------|------------|
| <页面> | <入口A> | <入口B> | 一致 / 差异说明 |

<示例——写作工具:>
| chapter_index.md | 阶段③卡片点击 | 文件树点击 | 阶段③=竖列视图，文件树=Markdown编辑器——双重呈现，见Spec §5 |

## 技术说明（可选）
<如果涉及以下内容，需要说明：>
- 外部依赖：需要调用什么服务？有什么限制？
- 特殊要求：离线使用？系统权限？性能要求？

## 补充说明
<如有需要，用表格说明选项、状态、逻辑等>

---

## 完整示例

以下是一个「剧本分镜生成器」的 Product Spec 示例，供参考：

```markdown
## 产品概述

这是一个帮助漫画作者、短视频创作者、动画团队将剧本快速转化为分镜图的工具。

**目标用户**：有剧本但缺乏绘画能力、或者想快速出分镜草稿的创作者。他们可能是独立漫画作者、短视频博主、动画工作室的前期策划人员，共同的痛点是「脑子里有画面，但画不出来或画太慢」。

**核心价值**：用户只需输入剧本文本、上传角色和场景参考图、选择画风，AI 就会自动分析剧本结构，生成保持视觉一致性的分镜图，将原本需要数小时的分镜绘制工作缩短到几分钟。

## 应用场景

- **漫画创作**：独立漫画作者小王有一个 20 页的剧本，需要先出分镜草稿再精修。他把剧本贴进来，上传主角的参考图，10 分钟就拿到了全部分镜草稿，可以直接在这个基础上精修。

- **短视频策划**：短视频博主小李要拍一个 3 分钟的剧情短片，需要给摄影师看分镜。她把脚本输入，选择「写实」风格，生成的分镜图直接可以当拍摄参考。

- **动画前期**：动画工作室要向客户提案，需要快速出一版分镜来展示剧本节奏。策划人员用这个工具 30 分钟出了 50 张分镜图，当天就能开提案会。

- **小说可视化**：网文作者想给自己的小说做宣传图，把关键场景描述输入，生成的分镜图可以直接用于社交媒体宣传。

- **教学演示**：小学语文老师想把一篇课文变成连环画给学生看，把课文内容输入，选择「动漫」风格，生成的图片可以直接做成 PPT。

## 功能需求

**核心功能**
- 剧本输入与分析：用户输入剧本文本 → 点击「生成分镜」→ AI 自动识别角色、场景和情节节拍，将剧本拆分为多页分镜
- 角色设定：用户添加角色卡片（名称 + 外观描述 + 参考图）→ 系统建立角色视觉档案，后续生成时保持外观一致
- 场景设定：用户添加场景卡片（名称 + 氛围描述 + 参考图）→ 系统建立场景视觉档案（可选，不设定则由 AI 根据剧本生成）
- 画风选择：用户从下拉框选择画风（漫画/动漫/写实/赛博朋克/水墨）→ 生成的分镜图采用对应视觉风格
- 分镜生成：用户点击「生成分镜」→ AI 生成当前页 9 张分镜图（3x3 九宫格）→ 展示在右侧输出区
- 连续生成：用户点击「继续生成下一页」→ AI 基于前一页的画风和角色外观，生成下一页 9 张分镜图

**辅助功能**
- 批量下载：用户点击「下载全部」→ 系统将当前页 9 张图打包为 ZIP 下载
- 历史浏览：用户通过页面导航 → 切换查看已生成的历史页面

## 全局交互模式

> 所有页面遵循统一的交互规范。各功能无需单独定义，引用本节即可。

**空态**（数据不存在或未创建时）：居中显示提示文字 + 可选行动按钮。
**加载态**（异步操作进行中）：短操作 skeleton 占位，长操作 skeleton + 进度文字。
**错误态**（操作失败）：内联错误块（暗红底 + 浅红边框 + 错误描述 + [重试] 按钮）。
**确认弹窗**（破坏性操作前）：居中遮罩 + 面板 + [取消]/[确认] 按钮。

**独立页面窗口 Chrome**（适用于所有全屏独立页面——全局默认）：
- 每个独立全屏页面的页头/工具栏必须包含窗口控制按钮（最小化/最大化/关闭）
- 页头/工具栏作为可拖拽区域，按钮需标注不可拖拽
- 关闭按钮行为按页面语义区分：主页面关闭→退出应用，子页面关闭→返回上级
- 非独立页面（弹窗/对话框/子视图）不设窗口控制按钮

**Platform 约束**（根据平台标注不可用的 API）：
- 标注平台特定的 API 限制（如 Electron 不支持 `window.prompt()`/`window.confirm()`）
- 标注替代方案（如使用内联输入+状态管理、自定义弹窗组件）

## UI 布局

### 整体布局
左右两栏布局，左侧输入区占 40%，右侧输出区占 60%。

### 左侧 - 输入区
- 顶部：项目名称输入框
- 剧本输入：多行文本框，placeholder「请输入剧本内容...」
- 角色设定区：
    - 角色卡片列表，每张卡片包含：角色名、外观描述、参考图上传
    - 「添加角色」按钮
- 场景设定区：
    - 场景卡片列表，每张卡片包含：场景名、氛围描述、参考图上传
    - 「添加场景」按钮
- 画风选择：下拉选择（漫画 / 动漫 / 写实 / 赛博朋克 / 水墨），默认「动漫」
- 底部：「生成分镜」主按钮，靠右对齐，醒目样式

### 右侧 - 输出区
- 分镜图展示区：3x3 网格布局，展示 9 张独立分镜图
- 每张分镜图下方显示：分镜编号、简要描述
- 操作按钮：「下载全部」「继续生成下一页」
- 页面导航：显示当前页数，支持切换查看历史页面

## 用户使用流程

### 首次生成
1. 输入剧本内容
2. 添加角色：填写名称、外观描述，上传参考图
3. 添加场景：填写名称、氛围描述，上传参考图（可选）
4. 选择画风
5. 点击「生成分镜」
6. 在右侧查看生成的 9 张分镜图
7. 点击「下载全部」保存

### 连续生成
1. 完成首次生成后
2. 点击「继续生成下一页」
3. AI 基于前一页的画风和角色外观，生成下一页 9 张分镜图
4. 重复直到剧本完成

## AI 能力需求

| 能力类型 | 用途说明 | 应用位置 |
|---------|---------|---------|
| 文本理解与生成 | 分析剧本结构，识别角色、场景、情节节拍，规划分镜内容 | 点击「生成分镜」时 |
| 图像生成 | 根据分镜描述生成 3x3 九宫格分镜图 | 点击「生成分镜」「继续生成下一页」时 |
| 图像理解 | 分析用户上传的角色和场景参考图，提取视觉特征用于保持一致性 | 上传角色/场景参考图时 |

## 技术方向

| 维度 | 选择 | 理由 |
|------|------|------|
| 产品类型 | Web | 创作工具，需要方便分享和协作，无需系统权限 |
| 推荐技术栈 | Next.js + TypeScript + Tailwind | 全栈框架，支持 API 路由调用 AI 服务，SSR 利于 SEO |
| 数据存储 | 本地（LocalStorage） | MVP 阶段无需登录，降低复杂度 |
| 部署方式 | 静态托管（Vercel） | 零运维，自动部署，免费额度够用 |

## 技术说明

- **图像生成**：调用 AI 图像生成服务，每次生成 9 张图约需 30-60 秒
- **文件导出**：支持 PNG 格式批量下载，打包为 ZIP 文件

## 补充说明

| 选项 | 可选值 | 说明 |
|------|--------|------|
| 画风 | 漫画 / 动漫 / 写实 / 赛博朋克 / 水墨 | 决定分镜图的整体视觉风格 |
| 角色参考图 | 图片上传 | 用于建立角色视觉身份，确保一致性 |
| 场景参考图 | 图片上传（可选） | 用于建立场景氛围，不上传则由 AI 根据描述生成 |
```

---

## 写作要点

1. **产品概述**：
   - 一句话说清楚是什么
   - **必须明确写出目标用户**：是谁、有什么特点、什么痛点
   - 核心价值：用了这个产品能得到什么
   
2. **应用场景**：
   - 具体的人 + 具体的情况 + 具体的用法 + 解决什么问题
   - 场景要有画面感，让人一看就懂
   - 放在功能需求之前，帮助理解产品价值

3. **功能需求**：
   - 分「核心功能」和「辅助功能」
   - 每条格式：用户做什么 → 系统做什么 → 得到什么
   - 写清楚触发方式（点击什么按钮）

3.5 **全局交互模式**：
   - 定义跨页面统一的空态/加载态/错误态/确认弹窗模板
   - **必须定义「独立页面窗口 Chrome」**：每个全屏独立页面的窗口控制按钮（最小化/最大化/关闭）和拖拽区域
   - 标注平台特有的 API 约束

4. **UI 布局**：
   - 先写整体布局（几栏、比例）
   - 再逐个区域描述内容
   - 控件要具体：下拉框写出所有选项和默认值，按钮写明位置和样式

5. **用户流程**：分步骤，可以有多条路径

6. **AI 能力需求**：
   - 列出需要的 AI 能力类型
   - **Token 用量精度**（涉及 AI 调用的产品必须定义以下 4 项）：
     1. 计费 tokenizer：按模型区分（Anthropic→claude-tokenizer, OpenAI→tiktoken, 其他→char/4 估算）
     2. 统计范围：是否包含 system prompt？是否包含 tool call 往返？
     3. 舍入规则：向上取整 / 四舍五入 / 截断
     4. 显示精度：显示为 "12.4K" 还是 "12,345 tokens"？切换频率（实时/会话结束）？
     → 缺任一项 → 🟡。仅 CLI 项目可豁免。
   - 说明具体用途
   - **写清楚在哪个环节触发**，方便开发理解调用时机

7. **技术方向**：
   - 产品类型 + 推荐技术栈 + 理由，用表格呈现
   - 数据存储方式和部署方式也写进表格
   - 这是给 dev-planner 和 dev-builder 的输入

8. **技术说明**（可选）：
   - 外部服务依赖、特殊要求
   - 只在有技术约束时写，没有就不写

9. **补充说明**：用表格，适合解释选项、状态、逻辑
